# LeetCode 182: Duplicate Emails

## Problem
Given a table `Person` with columns:
- `id` (int, primary key)
- `email` (varchar)

Write a query to report all duplicate emails.

## SQL Solution
```sql
SELECT email
FROM Person
GROUP BY email
HAVING COUNT(email) > 1;
